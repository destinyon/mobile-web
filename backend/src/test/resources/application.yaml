spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:${random.uuid};MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:

app:
  jwt-secret: test-secret
  admin:
    username: admin
    password: secret
    user-id: 2
  oss:
    sync-demo-assets: false
    endpoint: https://oss-cn-beijing.aliyuncs.com
    public-base-url: https://example.test
    bucket: test-bucket
    access-key-id: test-access-key
    access-key-secret: test-access-secret
  wx:
    app-id: test-app-id
    app-secret: test-app-secret
  netease:
    news:
      startup-sync: false
      scheduled-sync: false
